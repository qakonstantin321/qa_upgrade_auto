services:
  backend:
    image: nobugsme/nbank:with_validation_fix_with_swagger
    ports:
      - "4111:4111"
    networks:
      - nbank-network

  frontend:
    image: nobugsme/nbank-ui:with_nginx_with_send_transfer_fix
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - nbank-network

networks:
  nbank-network:
    name: nbank-network